url: "https://maikelthedev.github.io" # the base hostname & protocol for your site
baseurl: "" # the subpath of your site, e.g. /blog
title: "Maikelology" # the name of your site, e.g. ACME Corp.
author: "Maikel Frias Mosquea"
description: "Ramblings of @maikel@vmst.io"

# GitHub Pages compatible plugins
plugins:
  - jekyll-sitemap
  - jekyll-feed
  - jekyll-seo-tag
  - jekyll-reading-time
  - jekyll-archives
  # Note: jekyll-paginate-v2 may have issues on GitHub Pages
  # If pagination doesn't work, consider removing it
  - jekyll-paginate-v2

# Syntax highlighting is built-in with Rouge (no plugin needed)
highlighter: rouge

# Kramdown configuration for anchor links
kramdown:
  auto_ids: true
  heading_offset: 0
  syntax_highlighter: rouge

# Jekyll Archives configuration
jekyll-archives:
  enabled:
    - categories
    - tags
  layouts:
    category: archive
    tag: tag
  permalinks:
    category: /categories/:name/
    tag: /tag/:name/

# Jekyll Paginate V2 configuration
pagination:
  enabled: true
  per_page: 10
  permalink: '/page/:num/'
  title: ':title - page :num'
  limit: 0
  sort_field: 'date'
  sort_reverse: true
  trail:
    before: 2
    after: 2

# Feed configuration
feed:
  collections:
    posts:
      path: "/feed.xml"
